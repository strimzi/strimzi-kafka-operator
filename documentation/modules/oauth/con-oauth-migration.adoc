:_mod-docs-content-type: CONCEPT

[id='con-oauth-migration-{context}']
= Migrating to the `custom` authentication type

[role="_abstract"]
The `oauth` and `keycloak` authentication types are deprecated and will be removed in a future release.  
Kafka listeners and Kafka clients must now configure OAuth 2.0 using the `custom` authentication type.
OAuth 2.0 support remains the same; only the configuration model changes.

This change affects **both server-side listener configuration** and **client-side authentication** for Strimzi-managed components.

For more information on the configuration options, see the following API schema references:

* link:{BookURLConfiguring}#type-KafkaListenerAuthenticationCustom-reference[`KafkaListenerAuthenticationCustom` schema reference^]
* link:{BookURLConfiguring}#type-KafkaClientAuthenticationCustom-reference[`KafkaClientAuthenticationCustom` schema reference^]

== Handling sensitive values securely in `custom` authentication

With the `custom` authentication type, credentials such as OAuth client secrets or passwords are embedded directly in the `sasl.jaas.config` string inside the `config` section.

WARNING: Do not use sensitive values directly in the `config` section of `custom` authentication.

Use Kafka configuration providers to load sensitive values from Kubernetes `Secret` resources at runtime instead.
Configure the Strimzi `KubernetesSecretConfigProvider` in the component's `spec.config` and reference secrets using the template syntax `${<config-provider-alias>:<namespace>/<secret-name>:<key>}`.

.Insecure: OAuth client secret as a literal value in sasl.jaas.config
[source,yaml]
----
authentication:
  type: custom
  sasl: true
  config:
    sasl.mechanism: OAUTHBEARER
    sasl.jaas.config: |
      org.apache.kafka.common.security.oauthbearer.OAuthBearerLoginModule required
        oauth.client.id="my-client-id"
        oauth.client.secret="my-plaintext-client-secret"
        oauth.token.endpoint.uri="https://my-oauth-server/token";
    sasl.login.callback.handler.class: io.strimzi.kafka.oauth.client.JaasClientOauthLoginCallbackHandler
----

.Secure: OAuth client secret loaded from a Kubernetes Secret via a config provider
[source,yaml,subs="attributes+"]
----
spec:
  config:
    config.providers: my-secrets-provider # <1>
    config.providers.my-secrets-provider.class: io.strimzi.kafka.KubernetesSecretConfigProvider # <2>
  # ...
  authentication:
    type: custom
    sasl: true
    config:
      sasl.mechanism: OAUTHBEARER
      sasl.jaas.config: |
        org.apache.kafka.common.security.oauthbearer.OAuthBearerLoginModule required
          oauth.client.id="my-client-id"
          oauth.client.secret="${my-secrets-provider:mynamespace/my-oauth-secret:client-secret}" # <3>
          oauth.token.endpoint.uri="https://my-oauth-server/token";
      sasl.login.callback.handler.class: io.strimzi.kafka.oauth.client.JaasClientOauthLoginCallbackHandler
----
<1> A user-chosen alias for the provider, used in template references.
<2> `KubernetesSecretConfigProvider` loads values from Kubernetes `Secret` resources at runtime.
<3> A config provider template reference. The `KubernetesSecretConfigProvider` resolves it to the actual secret value at runtime, so the sensitive value is never stored in plaintext in the component configuration.

Replace `mynamespace` with the namespace where the component is deployed.
For more information on setting up access rights, see the link:{BookURLConfiguring}#assembly-loading-config-with-providers-str[Loading configuration values from external sources^] section.

