apiVersion: kafka.strimzi.io/v1beta2
kind: Kafka
metadata:
  name: missing-required-property
spec:
  cruiseControl:
    template:
      pod:
        metadata:
          annotations:
            app: kafka
