steps:
  - bash: ".azure/scripts/setup-kubernetes.sh"
    displayName: "Setup Kubernetes cluster"
    env:
      # we can use kind or minikube
      TEST_CLUSTER: minikube
      TEST_KUBECTL_VERSION: v1.21.0
      TEST_KUBERNETES_VERSION: v1.24.0
