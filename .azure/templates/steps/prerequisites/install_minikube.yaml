steps:
  - bash: ".azure/scripts/setup-minikube.sh"
    displayName: "Setup Minikube cluster"
    env:
      # we can use kind or minikube
      TEST_CLUSTER: minikube
      TEST_KUBECTL_VERSION: v1.21.0
      TEST_MINIKUBE_VERSION: v1.24.0
