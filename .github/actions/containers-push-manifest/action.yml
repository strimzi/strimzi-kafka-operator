name: "Push containers manifest"
description: "Push containers manifest"

runs:
  using: "composite"
  steps:
    - name: Push containers manifest
      shell: bash
      run: |
        make docker_push_manifest
