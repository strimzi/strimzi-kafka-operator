name: "Install Shellcheck"
description: "Install Shellcheck tool by our script"

runs:
  using: "composite"
  steps:
    - name: Install shellcheck
      shell: bash
      run: ".azure/scripts/setup_shellcheck.sh"
