name: Create Warning
description: "Creates a GitHub Actions workflow warning"

inputs:
  message:
    description: "Warning message to display"
    required: true

runs:
  using: composite
  steps:
    - name: Emit warning
      shell: bash
      env:
        WARNING_MESSAGE: ${{ inputs.message }}
      run: |
        echo "::warning::$WARNING_MESSAGE"